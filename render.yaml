services:
  - type: web
    name: vetsync-app
    runtime: php
    plan: free
    branch: main
    rootDir: .                # points to the root of your repo (vetsync)
    buildCommand: ""
    startCommand: "php -S 0.0.0.0:10000 -t ."